[project]
name = "dbt-foundations-sandbox"
version = "0.1.0"
description = "Local DuckDB sandbox for the reusable dbt Foundations training"
requires-python = ">=3.12,<3.13"
dependencies = [
  "dbt-core>=1.11,<1.12",
  "dbt-duckdb>=1.11,<1.12",
  "duckdb>=1.2.1,<2",
]

[tool.uv]
package = false
